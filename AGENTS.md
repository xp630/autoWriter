# autoWriter-desktop · 项目协作说明

> 写给"以后会读到这份文件的助手"，包括未来的我自己。
> 目标：让你（前缀/上下文）一上手就知道怎么跟这个项目的 owner 协作。
> **不是**产品功能说明——那些在 `docs/USER_GUIDE.md`。

---

## Owner 是谁

- **一个人**在做公众号：当前 2 粉，90 天目标 30 篇涨到 1000 粉
- 这个 Electron 应用是 ta 自己的写作工具——**ta 是唯一用户**，不是产品经理视角
- ta 在做这件事的同时还在迭代这个工具；**两个工作在并行**

---

## Owner 的工作风格（重要，照做）

- **短问题短回答**——长篇大论会被略读
- **挑刺型**——读你的稿子时"嗯？"会立刻指出，别怪 ta 不留情
- **先问"为什么"再决定**——不要给"因为好用""行业最佳实践"这种空话
- **先手写再决定系统要不要帮**——加新功能前先看 ta 自己能不能用手写流程跑通
- **看到过度工程化立刻叫停**——你如果在堆复杂度，自己先停下来想"这是不是过度设计"
- **接受"现在不行"的答案**——如果某件事现在不该做，明确说"现在不做"，比"以后做"好
- **会在对话里改主意**——上次不要的东西这次可能要，**别赌气"你之前说不要"**

---

## 已经定下来的方向（**不要重新讨论**）

> 这些 owner 已经在 CHANGELOG / WEEKLY_RECAP / PRODUCT_PLAN 里写过决定。**别再问"为什么不做排行榜"**。

- **唯一优先指标是"转发"**——"阅读≠认可，点赞≠价值，转发≈传播价值"。2 粉阶段就这么定
- **排行榜 / 评分 / 推荐系统**在 n<20 之前不做——这是反"伪确定感"的硬决定
- **闸门类功能**（证据账必填 / 三问必答 / 成稿体检）默认折叠 / 可选，**不作为硬拦截**
- **长文深度技术文章**我们系统写不出来，**也不打算让它写**——"AI 是放大器不是发明者"已确认
- **PR 流程已建立**——work on `feat/*` / `docs/*` / `fix/*` off `develop`，开 PR；不要绕过 branch protection
- **写文章代码已经"保留现状"**——V2/V3/V4 闸门不砍但默认折叠；不在 ta 拒绝之前主动重提
- **EP 正文格式（owner 定，2026-09-01）**：自然大段 3~6 段、纯文本输出；禁小标题、禁"金句一行一段"短句体——**目标是文字像人写的，不像排版的**。强调由 owner 在排版步点击改判加，生成时不加粗不切短句

---

## Owner 不会容忍的事

- AI **假装替你做决定**——给方案让 ta 选，不要自己决定
- **加新功能不解释为什么**——每次新增都要回答"解决谁的什么痛点"
- 用"行业洞察 / 最佳实践 / 业界共识"搪塞**—— ta 吃过亏，识别得出
- **改了文件不告诉 ta**——commit 之后说"改了什么、为什么"
- **过度承诺系统能力**——"系统能不能写这篇文章"类问题，**先看代码再回答**，不要凭印象

---

## 协作模式（每次新会话都用得上）

- **一问一答**，不要一次问 5 个问题——上一轮合作中已经验证好用
- **改代码前先说**："我要改什么、为什么、影响哪些文件"——获得口头 OK 再动手
- **大改动走 PR**（`feat/*`/`fix/*`/`docs/*` → develop），小改动（typo / 单文件 < 30 行）可以直接 commit develop
- **commit message 用中文**，写清"为什么"，不只写"做了什么"
- ta 问"这么做对吗"→ **先给真实判断，再说可选方案**。**不要列一堆选项让 ta 选**——这是上一轮被明确拒绝过的工作方式
- 如果 ta 在反思期 / 疲惫期 / 选了"先停一下"，**尊重这个状态**，不要主动推进
- 你看到 ta 在表达"不会用 / 太复杂 / 我不知道写什么"——**这是信号不是抱怨**。放慢，加解释，问 ta 想要什么；**不要继续堆功能**

---

## 项目快速状态

- **分支**：`develop` 主线，所有改动 PR 到这里
- **远程**：https://github.com/xp630/autoWriter.git
- **启动**：`npm run dev`，日志 `/tmp/aw-dev.log`
- **重启**：`pkill -f "electron \."; pkill -f concurrently; pkill -f vite; sleep 2; : > /tmp/aw-dev.log; nohup npm run dev > /tmp/aw-dev.log 2>&1 &`
- **数据库**：用户本地 `~/Library/Application Support/autowriter-desktop/autoWriter.db`（11 张表，V2/V3/V4 演进到 `content_strategies` + `strategy_articles`，不再有 `content_angles`）

---

## 文档地图（按用途读，不要全读）

| 文件 | 何时读 |
|---|---|
| `README.md` | 项目入口 / 安装 / 启动 |
| `docs/USER_GUIDE.md` | **用户视角**的功能说明（不是助手视角） |
| `docs/WEEKLY_RECAP.md` | 周记计划的 9 段模板 + 节奏——写周报时读 |
| `docs/PRODUCT_PLAN.md` | 产品方向 + 阶段规划 |
| `docs/MODULE_STATUS.md` | 模块成色（哪些是真实现，哪些是壳） |
| `docs/CHANGELOG.md` | 最近变更 + 待办 |
| `docs/CONTENT_STRATEGY_V2.md` | 内容策略层 V2 设计 |
| `docs/FEATURES.md` | 功能清单（不是用户文档） |
| `docs/P0_CONTENT_DECISION.md` | P0 内容层的决策记录 |
| `DESIGN.md` + `DESIGN.tokens.json` | 设计令牌真值源 |

---

## 这个项目目前的真实状态（写于 2026-08-29）

- **第一篇文章已发布**（`drafts/first-article-2026-08-29.html`）—— 标题"找了一篇硬核文章问 AI 能不能写，AI 说『它不背这个锅』"
- **周记计划已立**——双周一次，9 段模板，#1 还没写
- **写文章代码保留现状**——V2/V3/V4 闸门代码不删但默认折叠
- **本会话最后状态**：owner 决定把"做订阅号"做成长期系列选题；AGENTS.md 落地用于固化工作方式

下次新会话开始时，**先读这一段**——能少走 30 分钟弯路。
